# Spring JPA and MySQL configuration
spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/drustvenamreza_db?createDatabaseIfNotExist=true&useSSL=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Elasticsearch
spring.elasticsearch.uris=http://localhost:9200

# MinIO
minio.url=http://localhost:9000
minio.accessKey=ROOTUSER
minio.secretKey=CHANGEME123

# Logging
logging.file.name=logstash-ingest-data/application.log
# logging.level.com.example=DEBUG #(optional)

